{
  "name": "streamlit-zgyd-cloudflare-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker deployment dependencies.",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0" 
  }
}